[project]
name = "enter-presser"
version = "0.1.0"
description = "Press Enter/Return in the focused application after a delay."
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["pynput>=1.7.7"]

[project.scripts]
enter-presser = "enter_presser:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
